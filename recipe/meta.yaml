{% set name = "rpthermo" %}
{% set version = "6.8.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/brsynth/rpThermo/archive/refs/tags/{{ version }}.tar.gz
  sha256: 37230b72b5ee72c073ebd2fbd1fd8d6f2cd6d84351b0652890aa5d74005cbb14

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - scipy
    - equilibrator-api
    - brs_utils
    - chemlite
    - rplibs

test:
  requires:
    - python {{ python_min }}
    - pip
  imports:
    - rpthermo
  commands:
    - pip check

about:
  home: https://github.com/brsynth/rpThermo/tree/main
  summary: "Calculate the formation energy of chemical species and the Gibbs free energy for each reaction and for the heterologous pathway itself"
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ramez17khaled
    - breakthewall
    - tduigou
